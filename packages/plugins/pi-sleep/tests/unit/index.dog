use (
  alias://expected
  pkg = ~
)

suite(__filename, proc()
  test("when imported, api must be exported", proc()
    expected(pkg).toHave(
      plugin = "sleep"
      tags = ["built-in"]
      defaultOpName = "sleep"
    ).member("ops").toHave(
      "sleep"
    )
  end)
end)