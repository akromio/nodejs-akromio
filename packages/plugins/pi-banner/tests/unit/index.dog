use (
  alias://expected
  pkg = ~
)

suite(__filename, proc()
  test("when imported, api must be exported", proc()
    expected(pkg).toHave(
      plugin = "banner"
      tags = ["built-in"]
      defaultOpName = "banner"
    ).member("ops").toHave(
      "banner"
    )
  end)
end)