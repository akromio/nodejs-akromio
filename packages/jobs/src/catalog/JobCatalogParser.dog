use (
  {CatalogParser, JobParser} = alias://catalog
  Catalog = JobCatalog
)

/**
 * A parser to parse jobs catalog declarations.
 */
export struct JobCatalogParser: CatalogParser
  @override
  pub const jobParser = JobParser()

  @override
  pub fn createCatalog(decl: map) = Catalog(decl)