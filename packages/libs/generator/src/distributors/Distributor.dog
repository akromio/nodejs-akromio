/**
 * A distributor, that is, a component for notifying
 * the run requests to one or several destinations.
 */
export intf Distributor
  /**
   * Delivers the run requests among the ring destinations.
   */
  async fn start()
