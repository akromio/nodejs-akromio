/**
 * A blank sheet added to the stream by the starter.
 */
export intf BlankSheet
  /**
   * Timestamp when the blank sheet was added to the streaming.
   */
  ts: num

  /**
   * Internal job name such as, for example, __exit__.
   */
  job?: text
