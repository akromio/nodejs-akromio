use (
  alias://expected
  pkg = ~
)

suite(__filename, proc()
  test("when imported, its api must be exported", proc()
    expected(pkg).members(
      "Catalog"
      "CatalogMerger"
      "CatalogParser"
      "CatalogCo"
      "CatalogMacro"
      "JobParser"
    ).toBeCallable()
  end)
end)
