use (
  JobCatalog
)

/**
 * A catalog of jobs whose run is triggered by events.
 */
export struct TriggeredJobCatalog: JobCatalog
  /**
   * The triggers defined into the catalog.
   */
  pub const triggers := {}

  /**
   * Number of runners per engine.
   */
  pub const parallelism: num
