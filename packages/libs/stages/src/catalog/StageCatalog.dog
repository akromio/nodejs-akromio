use (
  {Catalog} = alias://catalog
)

/**
 * A catalog of stages.
 */
export struct StageCatalog: Catalog
  /**
   * Stages defined into this catalog.
   */
  pub const stages = {}

  /**
   * Default stage to run when not indicated explicitly.
   */
  pub const defaultStageName?: text
