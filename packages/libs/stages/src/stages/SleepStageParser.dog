use (
  SleepStage
  StageParser
)

/**
 * A parser for parsing stages associated a sleep starters.
 */
export struct SleepStageParser: StageParser
  @override @hidden
  pub fn _parse(decl: map) = decl{*, -sleep, name = decl.sleep, impl = "sleep"}
